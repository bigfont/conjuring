<!--Books-->
<section ng-controller="BookCtrl">
    <div class="page-header">
        <h1 id="books">Books</h1>
        <p>The <a href="http://saltspring.bc.libraries.coop/">Salt Spring Island Public Library</a> has the <strong>largest public library collection of magic books in BC!</strong></p>
    </div>
    <div class="row">
        <div class="col-md-4">
            <img class="img-responsive img-thumbnail img-stacked" src="Images/library-book-collection-1.jpg" alt="...">
        </div>
        <div class="col-md-4">
            <img class="img-responsive img-thumbnail img-stacked" src="Images/library-book-collection-2.jpg" alt="...">
        </div>
        <div class="col-md-4">
            <img class="img-responsive img-thumbnail img-stacked" src="Images/library-book-collection-3.jpg" alt="...">
        </div>
    </div>
    <div class="row">
        <div class="col-lg-6">
            <p>
                <div class="input-group">
                    <input class="form-control" id="Search" ng-model="search.$" placeholder="Search" />
                    <span class="input-group-addon">
                        {{ "Displaying " + (books | filter:search:bookSearchComparitor).length + " of " + books.length + " books." }}
                    </span>
                </div>
            </p>
        </div>
    </div>
    <div class="row">
        <div class="col-lg-12">
            <ul class="list-group">
                <li class="list-group-item {{ $odd ? 'odd-child' : 'even-child' }}" ng-repeat="book in books | filter:search:bookSearchComparitor | orderBy:'Title'">
                    <!--author-->
                    <span ng-show="book.LastName && !book.FirstName">{{ book.LastName }}</span>
                    <span ng-show="book.LastName && book.FirstName">{{ book.LastName + ", " + book.FirstName.substring(0,1) + "." }}</span>
                    <!--year-->
                    <span ng-show="book.PublishYear">{{ "(" + book.PublishYear + ")." }}</span>
                    <!--title-->
                    <i class="accent" ng-show="book.Title">{{ book.Title + "." }}</i>
                    <i class="accent" ng-show="book.Title && book.Subtitle">{{ book.Title + ": " + book.Subtitle + "." }}</i>
                    <!--publisher-->
                    <span ng-show="!book.PublishLocation && book.Publisher">{{ book.Publisher + "." }}</span>
                    <span ng-show="book.PublishLocation && book.Publisher">{{ book.PublishLocation + ": " + book.Publisher + "." }}</span>
                </li>
            </ul>
        </div>
    </div>
</section>
<!--End Books-->
